const initState = {
  balance: 0,
  address: "",
  transactions: [],
  tokens: []
};
function WalletReducer(state, action) {
  switch (action.type) {
  }
}

export { initState };

export default WalletReducer